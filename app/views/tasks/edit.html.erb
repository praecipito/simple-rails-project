<h1>Edit the task</h1>

<%= simple_form_for @task do |f| %>
  <%= f.input :description %>
  <%= f.input :deadline %>
  <div class="btn btn-primary">
    <%= f.button :submit %>
  </div>
<% end %>
<%= link_to "Back to task", @task %>
